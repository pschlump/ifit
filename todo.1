1. allow nesting
1. Allow for if-syntax to be other than in column 1
2. add expressions ( iOS || Android ), ( !XDesktop )
3. add else / elseif
4. -s JSON-Hash.json
	{ "abc": "../abc/" }
	Substitute for $$abc$$ -> ../abc/

